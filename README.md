# ImageForge

Retro image processing service.

## Features
- Upload images (JPEG, PNG, WebP, GIF, TIFF)
- Transform: rotate, grayscale, resize

## Run
```bash
docker build -t imageforge .
docker run -p 8000:8000 imageforge
```
